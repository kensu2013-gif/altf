# ALTF CRM Flow Chart 대시보드 및 이벤트 시스템 구현 계획서 (Implementation Plan)

본 설계서는 알트에프(ALTF) 플랫폼의 CRM 분석 기능 내에 'flow chart' 탭을 신설하고, 매출 추세 분석, 비즈니스 이벤트 기록, 재고 변동 연동성 감지, 지역별 실적 및 이상 업체 모니터링 기능을 구현하기 위한 상세 구현 계획서입니다.

---

## 1. 개요 및 설계 방향

*   **목표:** CRM 데이터를 기반으로 전사 매출 흐름을 시각화하고, 비즈니스 마커 이벤트를 매핑하여 매출 변동의 인과관계를 쉽게 파악할 수 있도록 돕습니다.
*   **재고 상관관계 감지:** 대경벤드/시화재고 변동 히스토리를 당일 매출 데이터와 대조하고 피어슨 상관계수를 산출하여 룰 기반 의사결정 근거를 도출합니다.
*   **업체 모니터링:** 최근 매출이 급상승한 '급부상 업체'와 평균 주문 주기 대비 지연이 발생한 '이탈 위험 업체'를 선제적으로 식별합니다.

---

## 2. 제안 변경 사항 (Proposed Changes)

### [MODIFY] [local-api-server.js](file:///Volumes/Extreme%20SSD/altf-kr-web-skeleton/local-api-server.js)
*   **CRM 이벤트 데이터 스페이스 구축:** `db.crmEvents` 배열을 기본 데이터 구조에 추가하고 S3 영구 저장소 연동 처리.
*   **신규 API 엔드포인트 구현:**
    *   `GET /api/crm/events`: 전체 이벤트 이력 조회.
    *   `POST /api/crm/events`: 이벤트 저장 (유형, 날짜, 제목, 설명 수집).
    *   `DELETE /api/crm/events/:id`: 특정 이벤트 삭제.

### [MODIFY] [Customers.tsx](file:///Volumes/Extreme%20SSD/altf-kr-web-skeleton/src/pages/admin/Customers.tsx)
*   **탭 리스트 확장:** `activeTab` 상태의 유형에 `'FLOW_CHART'`를 추가하고, 탭 바 UI에 `'flow chart'` 버튼을 렌더링.
*   **조건부 컴포넌트 마운트:** `'FLOW_CHART'` 활성화 시 신규 컴포넌트 `<FlowChartDashboard />`를 호출하고 필요한 데이터(`orders`, `customersList`, `inventoryMap`, `token`)를 전달.

### [NEW] [FlowChartDashboard.tsx](file:///Volumes/Extreme%20SSD/altf-kr-web-skeleton/src/components/admin/FlowChartDashboard.tsx)
*   **역할:** CRM Flow Chart 대시보드 최상위 뷰어 및 분석 엔진.
*   **핵심 세부 기능:**
    1.  **매출 추이 시각화 (SVG 차트):**
        *   올해 및 작년의 월별, 분기별, 반기별, 연간 전체 실적 데이터를 계산하여 꺾은선 + 막대 커스텀 SVG 차트로 표현.
        *   직전월 대비 현재월의 매출 증감률을 실시간 계산하여 대형 뱃지 및 KPI 카드로 상단에 강조 노출.
    2.  **비즈니스 이벤트 저장 및 타임라인 매핑:**
        *   그래프 영역 하단에 타임라인 형식의 이벤트 목록 뷰 구현.
        *   이벤트 등록 모달 폼 제공 (날짜, 이벤트 유형 [가격 변동, 대량 발주, 경쟁사 이탈, 기타], 이벤트명, 세부 설명).
        *   매출 그래프의 X축(시간선)에 이벤트 발생일자별 마커를 시각적으로 오버레이하여 매출 변동과의 인과관계를 쉽게 파악할 수 있도록 설계.
    3.  **대경벤드 & 시화재고 비교분석 및 매출 영향도 감지:**
        *   대경벤드 양산재고 변동 내역(`daekyungHistory`)과 우리 시화재고 변동 내역(`inventoryHistory`)을 수집.
        *   특정 품목이나 전체 수량 기준의 재고 증감과 해당 시점의 매출 발생 데이터를 크로스 매칭하여 듀얼 축 SVG 상관관계 차트 생성.
        *   "대경벤드 재고가 급감할 때 시화재고 발주가 X% 증가하는 경향"과 같은 룰 기반 자동 판단 근거(Rule-based Intelligence text)를 도출하여 상단에 노출.
    4.  **지역별 판매 및 급등/급락 업체 추적:**
        *   8도 기준 지역별 매출 점유율을 나타내는 SVG 도넛 차트 렌더링.
        *   **급부상 업체(갑자기 나타난 업체):** 최근 30일 이내에 거래가 신설되었거나, 직전월 대비 매출이 50% 이상 급격히 성장한 업체 목록 시각화.
        *   **이탈/발주 급락 업체:** 평균 발주 주기 대비 1.5배 이상 지연되고 있거나, 최근 45일간 발주가 없는 과거 우량 고객사를 자동으로 식별하여 "마케팅 조치 필요" 알림 및 점유율 하락 근거 제시.

---

## 3. 검증 계획 (Verification Plan)

### 자동화 테스트 및 정적 분석
*   정적 컴파일 검증: `npx tsc --noEmit` 실행하여 타입 안정성 확인.
*   린트 에러 검증: `npx eslint .` 실행하여 린트 위반 여부 확인.

### 수동 검증 및 동작 시나리오
1.  **차트 정상 로딩 및 필터링 검증:** 탭 클릭 시 월별, 분기별, 반기별 실적이 즉시 취합되어 SVG 그래프로 올바르게 표시되는지 확인.
2.  **이벤트 등록 및 매핑 검증:** 신규 이벤트를 추가했을 때 백엔드 S3 DB에 성공적으로 저장되고, 타임라인 및 차트의 해당 날짜 시점에 이벤트 마커가 동적으로 찍히는지 검사.
3.  **재고 매출 분석 확인:** 시화재고 및 대경재고 이력을 연동하여, 재고량 변화가 매출액 변화와 오버레이되어 상관 그래프 및 분석 문구가 도출되는지 대조.
4.  **지역/업체 감지 테스트:** 신규 등록 업체나 최근 45일간 주문이 끊긴 기존 업체를 정확히 식별하여 급상승/이탈경보 카드로 시각화되는지 확인.
